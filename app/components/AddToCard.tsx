'use client'
import React from 'react'

const AddToCard = () => {
  return (
    <div>
        <button onClick={() => console.log('Click to this button')}>Add to Card</button>
    </div>
  )
}

export default AddToCard